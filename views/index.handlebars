<div class="container">

    <div class="row">
        <div class="col s 12">
            <ul class="collapsible" data-collapsible="expandable">
    {{#each scraped}}
                <li>
                    <div class="collapsible-header"><i class="material-icons">description</i>{{headline}}</div>
                    <div class="collapsible-body"><span><a href="{{link}}" target="_blank">{{summary}}</a></span></div>
                </li>
                <li>
                    <div class="collapsible-header"><i class="material-icons">subject</i>Comments</div>
                    <div class="collapsible-body"><span>this should be some handlebars function to handle comments maybe a partial</span></div>
                </li>
                <li>
                    <div class="collapsible-header"><i class="material-icons">add</i>Add Comment</div>
                    <div class="collapsible-body">
                        <span>Add a Comment</span>
                        <form>
                            <textarea></textarea>
                            <button class="submit">Submit</button>
                        </form>
                    </div>
                </li>
    {{/each}}
            </ul>
        </div>
    </div>

</div>

<script src="./assets/js/app.js"></script>